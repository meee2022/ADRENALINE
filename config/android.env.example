# Copy this file to client/.env.android.local. Never commit the real file.
VITE_CONVEX_URL=https://YOUR-PRODUCTION-DEPLOYMENT.convex.cloud

# Change to true only after checking that the deployment contains the live
# subscriber data and is the intended long-term Google Play backend.
ANDROID_RELEASE_CONFIRMED=false
