# Copy this file to .env.ios.local. Never commit the real file.
VITE_CONVEX_URL=https://YOUR-PRODUCTION-DEPLOYMENT.convex.cloud

# Change to true only after checking that the deployment contains the live
# subscriber data and is the intended long-term App Store backend.
IOS_RELEASE_CONFIRMED=false
