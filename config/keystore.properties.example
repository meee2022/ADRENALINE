# Copy to android/keystore.properties. NEVER commit the real file, and keep a
# backup of the .jks itself — losing it means you can never update the app.
storeFile=../adrenaline-release.jks
storePassword=
keyAlias=adrenaline
keyPassword=
