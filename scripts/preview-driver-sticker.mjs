import fs from 'node:fs';
import path from 'node:path';
const root = process.cwd();
const source = fs.readFileSync(path.join(root,'client/src/pages/Stickers.tsx'),'utf8');
const start = source.indexOf('        .label {');
const end = source.indexOf('        @media print {', source.indexOf('        .cust-num-inline {', start));
let css = source.slice(start,end);
if (css.includes('${') || start < 0 || end < 0) throw new Error('Sticker stylesheet boundaries changed');
const heart = fs.readFileSync(path.join(root,'client/public/heart-logo.png')).toString('base64');
const label = (code='') => `<div class="label label-box" dir="ltr"><div class="seq-mark">1</div>${code?`<span class="driver-code">${code}</span>`:''}<div class="brand-block"><span class="box-no-top">127</span><div class="brand-text"><div class="brand-name">ADRENALINE</div><div class="brand-tag">HEALTHY FOOD</div></div><img class="brand-heart" src="data:image/png;base64,${heart}" alt=""></div><div class="brand-rule"></div><div class="content-center"><div class="cust-line" dir="rtl">عميل تجريبي</div><div class="cust-sub"><span class="cust-phone">0000 0000</span></div><div class="plan-box"><div class="plan-txt">2 MAIN MEALS + 1 SNACK</div></div></div><div class="date-row"><div class="date-cell"><div class="date-label">DATE</div><div class="date-value">14/09/2026</div></div><div class="date-divider"></div><div class="date-cell"><div class="date-label">SHIFT</div><div class="date-value">Morning</div></div><div class="date-divider"></div><div class="date-cell"><div class="date-label">GOAL</div><div class="date-value">FITNESS</div></div></div></div>`;
const html = `<!doctype html><html lang="ar" dir="rtl"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>تجربة كود السائق</title><style>
${css}
:root{--label-w:58mm;--label-h:39mm}*{box-sizing:border-box}body{margin:0;background:#f1f5f9;color:#0f1516;font-family:Tahoma,Arial,sans-serif;padding:40px 24px}main{max-width:980px;margin:auto}h1{font-size:28px;margin:0 0 12px}p{line-height:1.9;color:#47759c;margin:0 0 28px}.compare{display:flex;gap:40px;flex-wrap:wrap;justify-content:space-between}section{flex:1;min-width:280px}h2{font-size:17px;margin:0 0 20px}.stage{height:300px;display:flex;align-items:center;justify-content:center;background:#fff;border-radius:12px}.label{zoom:1.8}.label .brand-heart{filter:grayscale(1) brightness(0)!important}.label .plan-box{background:white;border-color:black}.label .plan-txt{color:black!important;-webkit-text-fill-color:black!important}.driver-code{position:absolute;right:1.2mm;top:2mm;border:.7px solid black;border-radius:.7mm;font:700 9px Tahoma,sans-serif;padding:.45mm .65mm;line-height:1;color:black;background:white}.note{font-size:14px;margin-top:24px}.size{font-size:12px;margin-top:12px;color:#47759c}@media(max-width:600px){body{padding:24px 16px}.compare{gap:24px}.label{zoom:1.35}.stage{height:235px}}@media print{body{background:white}.label{zoom:1}.stage{height:50mm}h1{font-size:16px}}
</style><main><h1>نفس الستيكر، إضافة واحدة صغيرة</h1><p>كود السائق فقط — اسم العميل ورقمه وباقي التفاصيل كما هي.</p><div class="compare"><section><h2>الحالي</h2><div class="stage">${label()}</div></section><section><h2>المقترح · كود السائق أعلى اليمين</h2><div class="stage">${label('D01')}</div></section></div><p class="size">مقاس القالب الحالي 58 × 39 مم · المعاينة مكبّرة للتوضيح.</p><p class="note">بيانات تجريبية فقط. D01 مثال غير مربوط بسائق فعلي. لم تتغير الطباعة أو بيانات التوصيل.</p></main></html>`;
const out = path.join(root,'output/driver-sticker-preview');
fs.mkdirSync(out,{recursive:true});fs.writeFileSync(path.join(out,'index.html'),html);
console.log(path.join(out,'index.html'));
