# Delivery tracking follow-up

- Native route: `apps/mobile/app/track/[token].tsx`.
- Paste a private Adrenaline `/track/` link from the order-tracking screen.
- State refreshes every 30 seconds while focused; stale driver locations suppress ETA.
- Map still opens the official website. Embedded native map remains pending.
- A valid delivery tracking link is required for real-order verification; phone `30296555` alone is insufficient.
- Never manufacture a link, submit an order, or change delivery status to obtain test data.
- TypeScript and allowlist checks passed; real delivery and device verification remain pending.
